##template setup file.

#environment to use. must be either 'production' or 'development'
env: 'development'
#path to database
db_path: 'sqlite:////db/newvalley-dev.db'
#address to serve
host: "0.0.0.0"
port: 5000
#number of worker threads
n_workers: 1
